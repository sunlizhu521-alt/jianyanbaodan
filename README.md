# 检验报告单编码

当前仓库只放置从 `beihuo` 页面迁移来的静态前端框架。

已保留：

- 左侧导航、顶部标题区、指标卡、文件槽位、筛选区和明细表版式
- 与原页面接近的响应式布局和基础视觉样式

未包含：

- `file-library.js`、`shared-library.js`
- SheetJS、JSZip 等 vendor 脚本
- `data/shared-library.json`
- Excel 解析、IndexedDB 缓存、文件上传、筛选联动、下载导出等业务逻辑

直接打开 `index.html` 即可查看静态框架。
